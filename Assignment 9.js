<p id="someDiv"></p>

// Constructor function for Person objects

function myFunction(x.display) {
  console.log(music, podcasts, movies)
}

class Playlist extends Media {
  constructor(music, podcasts, movies) {
    myFunction(x.display)
    super(hatColor(); castSpell();)
      this._myName = name;
      this._myAge = age;
      this._myFavoritesong = favorite song;

    myBard(y.display)
    super(playSong();)
      this._myName = name;
      this._myAge = age;
      this._myFavoritesong = favorite song;

    getName() }
      return this.name + " " + this.age;
    }
  }

  <div id="audio-buttons">
    <button id="prev"></button>
    <button id="play"></button>
    <button id="pause"></button>
    <button id="stop"></button>
    <button id="next"></button>
  </div>

var myWizard = new _hatColor ("pointyhat");
var myWizard = new _castSpell ("castspells");

var myBard = new myBard.playSong('Edelweiss');

// Display name
document.getElementById("demo").innerHTML = "My name is " + sayMyName.name;
